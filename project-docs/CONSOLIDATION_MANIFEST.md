# Frozen Laptop Baseline Manifest

## Runtime included

- complete v0.19.2 Core, Gateway, and Tools source;
- Windows native bring-up, readiness, status, restart, and diagnostics;
- approved persona v3 and Browser Senses fallback voice profile;
- native tool schemas, side-effect-free preflight, exact-once approvals;
- deterministic post-action receipts and failed-action retry stop;
- bounded read-only auto-approval for `read`, `glob`, and `grep`;
- signed one-tap Telegram approval controls bound to trusted Founder and chat;
- central `CommandRegistry`, generated `/help`, handlers, and Bot command menu;
- persistent trust-observation epoch;
- portable Windows OS evidence fallback;
- complete state inspector v2;
- archive-only original-brain preservation boundary;
- rebuilt Core, Tools, and Gateway wheels from this exact tree.

## Preservation evidence included

- current laptop `AETHER_HOME` profile;
- original-brain dry-run SHA-256 manifest;
- legacy database profile;
- legacy claim-registry profile;
- inactive legacy skill-candidate map;
- accepted preservation decision.

These files contain metadata and hashes, not copied secrets.

## Intentionally not active

- Google Gemini TTS provider adapter;
- Agent-Reach bridge;
- Windows persistent service installer;
- Cloudflare production route;
- public shipping adapters for v0.20;
- automatic original-brain semantic import.
